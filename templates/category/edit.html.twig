{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <h1>Edit Category</h1>

    {# Inclure le formulaire d'édition avec un bouton "Update" #}
    {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_category_index') }}" class="btn btn-secondary mt-3">Back to list</a>

    {# Inclure le formulaire de suppression (généralement un petit formulaire avec un bouton) #}
    {{ include('category/_delete_form.html.twig', {'category': category}) }}
{% endblock %}